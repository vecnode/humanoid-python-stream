# Audio worker dependencies — install into .venv (Python 3.10+)
#
# Setup (one time):
#   python3.10 -m venv .venv          # or: python3 -m venv .venv
#   .venv/bin/pip install -r audio_runtime/requirements_worker.txt
#   .venv/bin/pip install flash-attn --no-build-isolation   # optional fast path
#
# The launch script starts the worker automatically when .venv/bin/python exists.

# VibeVoice TTS model + streaming extras
vibevoice[streamingtts] @ git+https://github.com/microsoft/VibeVoice.git

# HuggingFace stack (vibevoice pins these transitively, listed for clarity)
transformers>=4.51.3
huggingface-hub>=0.23
safetensors>=0.4
accelerate>=0.30

# Torch — must match the CUDA version installed on the machine.
# If you already have torch in the system/another env, you can skip this.
torch>=2.2.0

# Audio playback (optional — worker saves .wav files if sounddevice is absent)
sounddevice>=0.4.6
